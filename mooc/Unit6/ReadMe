这一单元主要学习c++继承相关的知识

Inheritance.cpp
继承的一些知识
1.默认不使用基类的构造函数，要显示调用Using Parent::Parent();
2.若基类ctor未被显式调用，基类的默认构造函数就会被调用
3.构造类实例会沿着继承链调用所有的基类ctor(dtor相反)

NameHidingInInheritance.cpp
继承中的名字隐藏
1.基类同名函数被隐藏
2.内部作用域的名字隐藏外部作用域的(同名)名字